"use strict";
/*
  |--------------------------------------------------------------------------
  | Language constants
  |--------------------------------------------------------------------------
*/
const langConstants = Object.freeze({
  locale: ["en", "es"],
  default_locale: "en",
});

export { langConstants };
